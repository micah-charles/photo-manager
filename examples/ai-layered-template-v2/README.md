# Layered template example

This directory is a metadata-only, privacy-safe example. Replace the catalog
and asset IDs with the IDs from an exported Photo Manager package and add the
three PNG files described by `manifest.json` before importing. The automated
tests generate synthetic foreground/mask PNGs; no personal photos or local
absolute paths are stored in this repository.
